# JSON-compiler
Basic front-end compiler in Python to perform lexical, syntactic, and semantic analysis on JSON structures. This project implemented a recursive descent parser to process input token streams, detect errors, and construct abstract syntax trees.
